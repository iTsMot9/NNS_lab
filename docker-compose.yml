services:
  jupyter:
    build: .
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/home/jovyan/work
      - ./data:/home/jovyan/data 
    environment:
      - JUPYTER_TOKEN=1234
    restart: unless-stopped
